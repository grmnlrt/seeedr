
  // if @exhibition.errors.any?
  //   // Render new review form with errors
  //   $('#new_exhibition').html("<%= j render 'new/_form/_min_price', exhibition: @exhibition, review: @review %>");
  // else
    // Create a paragraph for the new min-price and show it.
